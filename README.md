# aalpin-alarm-status

This app uses flask, nginx, and uWSGI.

Run 
```bash
docker-compose up -d
``` 
to start the application.

## Update
To update the application, run 
```
sudo bash ./update.sh
```